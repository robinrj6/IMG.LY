// A collection of configuration values that control the default behavior of various components.

export const CONFIG = {
  // This value will either display or hide the "Hiring" and "Remote works! Join us." banner.
  hiring: false,
};
